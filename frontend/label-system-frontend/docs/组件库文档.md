# 标签打印系统 - 组件库文档

## 🚀 概述

标签打印系统组件库是基于 Vue 3 + TypeScript + Composition API 构建的企业级 UI 组件库，专门为标签打印系统设计。组件库包含布局组件、基础UI组件和业务组件三大类别。

## 📦 安装与使用

### 全局安装
```typescript
// main.ts
import { createApp } from 'vue'
import App from './App.vue'
import LabelUI from './components'

const app = createApp(App)
app.use(LabelUI)
app.mount('#app')
```

## **按需引入**

```TYPESCRIPT
import { Button, Input, LabelEditor } from '@/components'

export default {
  components: {
    Button,
    Input,
    LabelEditor
  }
}

```

## 🎨 组件列表

### 布局组件

- `AppLayout` - 主布局框架
- `Header` - 页头组件
- `Sidebar` - 侧边栏组件
- `Footer` - 页脚组件

### 基础UI组件

- `Button` - 按钮组件
- `Input` - 输入框组件
- `Select` - 选择器组件
- `Table` - 表格组件
- `Dialog` - 对话框组件
- `Card` - 卡片组件

### 业务组件

- `LabelEditor` - 标签编辑器
- `PrintQueue` - 打印任务队列
- `DeviceStatus` - 设备状态显示
- `DataChart` - 数据图表

### 组合式函数

- `useForm` - 表单验证和管理
- `useTable` - 表格数据处理
- `usePrint` - 打印相关逻辑

## 🔧 开发指南

### 组件开发规范

1. 使用 Composition API 和 `<script setup>` 语法
2. 所有组件必须提供 TypeScript 类型定义
3. 组件样式使用 scoped CSS 和 CSS 变量
4. 遵循单一职责原则，保持组件简洁

### 性能优化

- 使用 `shallowReactive` 和 `shallowRef` 减少不必要的响应式嵌套
- 利用 Vue 3.6+ 的 Vapor Mode 和 Alien Signals 特性
- 实现 Tree Shaking 支持，按需加载

### 代码规范

- 使用 ESLint + Prettier 保持代码风格一致
- 编写完整的单元测试
- 提供详细的组件文档和示例

## 📱 响应式设计

组件库支持以下断点：

- xs: < 576px (移动端)
- sm: ≥ 576px (平板)
- md: ≥ 768px (桌面端)
- lg: ≥ 992px (大屏幕)
- xl: ≥ 1200px (超大屏幕)

## 🎯 主题定制

组件库支持通过 CSS 变量进行主题定制：

```css
:root {
  --primary-color: #409eff;
  --success-color: #67c23a;
  --warning-color: #e6a23c;
  --danger-color: #f56c6c;
  --info-color: #909399;
}

```

## 📄 组件示例

### Button 组件

```vue
<template>
  <Button
    type="primary"
    size="medium"
    :loading="isLoading"
    @click="handleClick"
  >
    点击我
  </Button>
</template>

<script setup>
import { Button } from '@/components'
import { ref } from 'vue'

const isLoading = ref(false)

const handleClick = () => {
  isLoading.value = true
  setTimeout(() => {
    isLoading.value = false
  }, 1000)
}
</script>

```

### useForm 组合式函数

```TYPESCRIPT
import { useForm } from '@/components/composables/useForm'

interface LoginForm {
  username: string
  password: string
}

const loginForm = useForm<LoginForm>({
  username: '',
  password: ''
})

// 设置验证规则
loginForm.setValidation('username', {
  required: true,
  validator: (value) => value.length >= 3 ? true : '用户名至少3个字符'
})

// 处理提交
loginForm.handleSubmit(async (values) => {
  console.log('表单数据:', values)
})

```

## 🔗 相关链接

- [Vue 3 官方文档](https://vuejs.org/)
- [TypeScript 官方文档](https://www.typescriptlang.org/)
- [Element Plus 组件库](https://element-plus.org/)
- [VueUse 组合式函数库](https://vueuse.org/)

## 📝 版本记录

### v1.0.0 (2024年)

- 初始版本发布
- 包含基础UI组件和业务组件
- 支持 TypeScript 和 Composition API
- 响应式设计支持

## 🤝 贡献指南

欢迎提交 Issue 和 Pull Request！请确保：

1. 遵循现有的代码风格
2. 添加相应的测试用例
3. 更新相关文档
4. 通过所有测试

## 📄 许可证

MIT License

Copyright (c) 2025 标签打印系统团队